<div>
    <h3>{{name}}</h3>
    {{#each Flashcards}}
    <h5>{{name}}</h5>
    <p>{{front}}</p>
    <p>{{back}}</p>
    <button class="del-btn" id="{{id}}">Delete</button>
    {{/each}}
</div>
<script src="../js/deckedit.js"></script>